{
  "name": "playwright-bdd-poc",
  "version": "1.0.0",
  "description": "Demo POC: Playwright + TypeScript + BDD con arquitectura en capas (page objects, servicio API, fixtures)",
  "main": "index.js",
  "scripts": {
    "test": "npx bddgen && npx playwright test",
    "test:ui": "npx bddgen && npx playwright test --project=ui",
    "test:api": "npx bddgen && npx playwright test --project=api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.20.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.1",
    "playwright-bdd": "^9.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  }
}
